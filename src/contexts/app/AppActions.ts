/**
 * All of the actions that can be dispatched to the AppContext
 */
export const ACTION_SET_BUSINESS_ID = "SET_BUSINESS_ID" as const;
export const ACTION_SET_APP_INIT = "SET_APP_INIT" as const;
export const ACTION_SET_USER = "SET_USER" as const;
